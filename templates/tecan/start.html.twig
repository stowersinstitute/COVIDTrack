{% extends 'base.html.twig' %}

{% block page_title %}Tecan to Specimen IDs{% endblock %}

{% block body %}
<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-body">
                <div class="box box-solid box-primary">
                    <div class="box-header">
                        <h3 class="box-title">Usage</h3>
                    </div>
                    <div class="box-body">
                        <p>Upload Tecan file containing Tube Accession IDs.</p>
                        <p>This upload accomplishes two things:</p>
                        <ol>
                            <li>Import Well Plate and Position information for Specimens into COVIDTrack</li>
                            <li>Transform Tube Accession IDs into Specimen Accession IDs and allow downloading the updated version</li>
                        </ol>
                        <p>The upload parser has these expectations:</p>
                        <ul>
                            <li>Uploaded file is comma-delimited or tab-delimited</li>
                            <li>Tube data begins on Row 3</li>
                            <li>Cell I2 (that's i2) contains the RNA Well Plate ID (Such as RPE1P7)</li>
                            <li>Column A contains the RNA Well Position (Begin at cell A3)</li>
                            <li>Column F contains the Tube Accession ID (Begin at cell F3)</li>
                        </ul>
                    </div>
                </div>

                {% if errors|length > 0 %}
                <div class="box box-solid box-danger">
                    <div class="box-header">
                        <h3 class="box-title">Conversion Errors</h3>
                    </div>
                    <div class="box-body">
                        <p>Could not complete conversion:</p>

                        <ul>
                        {% for error in errors %}
                            <li>{{ error }}</li>
                        {% endfor %}
                        </ul>
                    </div>
                </div>
                {% endif %}

                {{ form(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
