{% extends 'base.html.twig' %}

{% block page_title %}
    Print Group Badge Labels{% if group is defined %}: {{ group.title }}{% endif %}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="box box-primary">
                <div class="box-body">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}