{% block body %}
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>&nbsp;</th>
            <th>Title</th>
            <th>Accession ID</th>
            <th>Participants</th>
            <th>Active?</th>
            <th>Created</th>
            <th>Last Modified</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    {% for group in groups %}
        <tr>
            <td>
                <a href="{{ path('app_participant_group_edit', {title: group.title}) }}">Edit</a>
            </td>
            <td>
                <a href="{{ path('app_participant_group_view', {title: group.title}) }}">{{ group.title }}</a>
            </td>
            <td>{{ group.accessionId }}</td>
            <td>{{ group.participantCount }}</td>
            <td>
                {% if group.isActive() %}
                    <i class="fa fa-check text-green"></i>
                {% endif %}
            </td>
            <td>{{ group.createdAt | date("Y-m-d H:i:s") }}</td>
            <td>{{ group.updatedAt | date("Y-m-d H:i:s") }}</td>
            <td><a href="{{ path('app_participant_group_print', {title: group.title}) }}" title="Print Participant Group Labels"><i class="fa fa-fw fa-print"></i></a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
