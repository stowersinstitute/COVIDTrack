{% extends 'base.html.twig' %}

{% block page_title %}Participant Groups{% endblock %}

{% block body %}
<div class="row">
    <div class="col-xs-12">
        <a href="{{ path('app_participant_group_new') }}" class="btn btn-primary pull-right"><i class="fa fa-fw fa-plus"></i> New Participant Group</a>
    </div>
</div>

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-body">
                {% include('participantGroup/participant-group-list-table.html.twig') %}
            </div>
        </div>
    </div>
</div>

{% endblock %}
