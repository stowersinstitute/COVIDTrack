{% extends 'base.html.twig' %}

{% block page_title %}Participant Groups{% endblock %}

{% block body %}
<div class="row margin-bottom">
    <div class="col-md-12">
        <div class="pull-right">
            <a href="{{ path('app_participant_group_new') }}" class="btn btn-primary"><i class="fa fa-fw fa-plus"></i> New Participant Group</a>
            <a href="{{ path('group_excel_import') }}" class="btn btn-primary"><i class="fa fa-fw fa-plus"></i> Import from Excel</a>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="box">
            <div class="box-body">
                {% include('participantGroup/participant-group-list-table.html.twig') %}
            </div>
        </div>
    </div>
</div>

{% endblock %}
