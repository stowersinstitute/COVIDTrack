{% extends 'base.html.twig' %}

{% block page_title %}
    {% if new %} New Participant Group {% else %} Edit Participant Group {{ group.title }} {% endif %}
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-6">
        <div class="box box-primary">
            <div class="box-body">
                {{ form(form, {
                    'attr': {
                        'autocomplete': 'off',
                    }
                }) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
