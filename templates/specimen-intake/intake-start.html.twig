{% extends 'base.html.twig' %}

{% block page_title %}Specimen Intake{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <a href="{{ path('app_specimen_new') }}" class="btn btn-primary pull-right"><i class="fa fa-fw fa-plus"></i> New Specimen</a>
        </div>
    </div>

    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-body">
                    {% include('specimen/specimen-list-table.html.twig') %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
