{% extends 'base.html.twig' %}

{% block page_title %}Add User from LDAP{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-3">
            <div class="box box-primary">
                <div class="box-body">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-3">
            <a href="{{ path('user_new', { forceLocal: true }) }}" class="btn btn-default">Add a local user instead</a>
        </div>
    </div>
{% endblock %}