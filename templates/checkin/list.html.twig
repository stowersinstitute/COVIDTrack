{% extends 'base.html.twig' %}

{% block page_title %}Check-In Queue{% endblock %}

{% block body %}

<div class="row">
    <div class="row margin-bottom">
        <div class="col-md-9">
            <div class="pull-right">
                <a href="{{ path('checkin_import_start') }}" class="btn btn-primary"><i class="fa fa-fw fa-upload"></i> Import Accepted/Rejected</a>
            </div>
        </div>
    </div>

    <div class="col-md-9">
        <div class="box">
            <div class="box-body">
                {% include('checkin/list-table.html.twig') %}
            </div>
        </div>
    </div>
</div>

{% endblock %}
